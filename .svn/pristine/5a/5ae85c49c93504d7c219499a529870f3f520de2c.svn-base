import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BeficiaryTabComponent } from './beficiary-tab.component';

describe('BeficiaryTabComponent', () => {
  let component: BeficiaryTabComponent;
  let fixture: ComponentFixture<BeficiaryTabComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BeficiaryTabComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BeficiaryTabComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
