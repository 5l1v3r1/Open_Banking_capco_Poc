import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OtherBeneficiaryComponent } from './other-beneficiary.component';

describe('OtherBeneficiaryComponent', () => {
  let component: OtherBeneficiaryComponent;
  let fixture: ComponentFixture<OtherBeneficiaryComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OtherBeneficiaryComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OtherBeneficiaryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
